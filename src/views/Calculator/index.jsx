import React from "react";
import { Block, Container, Grid } from "../../components";

const Calculator = () => {
    return (
        <div>
            <Container>
                <Grid columns={2}>
                    <Block p={60}>

                    </Block>
                </Grid>
            </Container>
        </div>
    );
};

export default Calculator;